import { Button } from "@headlessui/react";

export default function AvatarButton({
  src,
  square = false,
  initials,
  href
}: {
  src?: string;
  square?: boolean;
  initials?: string;
  href?: string;
}) {
  return (
    <Button></Button>
  );
}