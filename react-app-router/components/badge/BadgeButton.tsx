import { Button } from "@headlessui/react";

export default function BadgeButton({
    color = 'zinc',
    href
}: {
    color?: string;
    href?: string;
}) {
    return (
        <Button></Button>)
}